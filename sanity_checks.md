1. There should be 112 columns.
2. Starting 2011 `forceuse` was added as a variable.
3. across years, some column titles are lowercase, but upper case for others.
4. Verify that for each year, for each column, 
    no more than 10% of data are missing, 
    except for the `forceuse` column and the 2014 dataset.


# To find weird stuff
1. Run table on all categorical variables.
2. Run summary on all variables to find extreme/non-sensical values.
3. 
